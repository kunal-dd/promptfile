---
model: claude-opus-4-8
provider: anthropic
input:
  text: string
output:
  title: string
  sentiment: enum(positive, neutral, negative)
  topics: string[]
  entities[]:
    name: string
    kind: enum(person, place, org)
---
<system>
Extract structured metadata from the user's text.
</system>

<user>
{{text}}
</user>
